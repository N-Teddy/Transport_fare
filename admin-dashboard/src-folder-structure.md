

```
src/
├── App.jsx
├── index.css
├── main.jsx
├── api/                    (empty directory)
├── assets/
│   └── react.svg
├── components/             (empty directory)
├── context/                (empty directory)
├── hook/                   (empty directory)
├── providers/              (empty directory)
└── utils/                  (empty directory)
```
